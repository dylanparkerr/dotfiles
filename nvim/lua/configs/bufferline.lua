require("bufferline").setup({
    options = {
        -- separator_style = "slant",  -- doesnt work on linux rn
        offsets = {{filetype = "NvimTree", text = "File Explorer", text_align = "center"}},
    }
})
