require('nvim-tree').setup{
    --todo add config
    renderer = {
        indent_markers = {
            enable = true
        },
        icons = {
            git_placement = 'after'
        }
    }
}
